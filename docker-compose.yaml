
version: "3.7"
services:
  frontend:
    build: ./frontend
    ports:
      - '8000:8000'
    volumes:
      - ./frontend/src:/app/src
     
    
  backend:
    build: ./backend
    ports:
    - 4000:4000
    environment:
      DB_URL: mongodb://db/Csteam
    volumes:
      - ./backend:/

volumes:
  Csteam:
  data:
